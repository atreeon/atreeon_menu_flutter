name: atreeon_menu_flutter
description: Displays a series of menus based on single pages for demoing purposes.
version: 0.0.1

environment:
  sdk: ">=2.12.0-0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true